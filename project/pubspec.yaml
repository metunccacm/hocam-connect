name: project
description: "METU NCC Super APP"
publish_to: 'none'

version: 0.0.8+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:

  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.11
  cryptography: ^2.5.0
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.3.0
  uuid: ^4.4.0
  provider: ^6.1.1
  supabase_flutter: ^2.9.1
  shimmer: ^3.0.0
  email_validator: ^2.1.17
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.3.1
  intl: ^0.20.2
  flutter_slidable: ^4.0.1
  equatable: ^2.0.5
  image_picker: ^1.1.0
  flutter_form_builder: ^10.2.0
  webview_flutter: ^4.7.0
  webview_flutter_web: ^0.2.2+4   # or latest
  html: ^0.15.6
  device_info_plus: any
  package_info_plus: any
  # cryptography_flutter: ^2.3.2 # unnecessary for now and affecting android build
  mime: ^2.0.0
  flutter_svg: ^2.2.1
  connectivity_plus: ^7.0.0
  internet_connection_checker_plus: ^2.9.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1
  firebase_core: ^3.8.1
  collection: ^1.19.1
  path: ^1.9.1
  liquid_swipe: ^3.1.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: "^0.13.1"

flutter:
  uses-material-design: true

  assets:
     - assets/
     - assets/logo/
     - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo/hc_logo.png"
  min_sdk_android: 21
